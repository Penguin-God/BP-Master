using static TestHelper;
using NUnit.Framework;

public class SkillEvaluateTests
{
    [Test]
    public void 정적_가치_스킬은_현재_적용_시_점수를_계산()
    {
        //var skill = CreateValueSkillData(SkillType.AttackChanger, 100, default, new SkillTargetRule(Side.All, TargetRange.All));
        //var sut = new StaticSkillEvaluator(new TargetCountCalculator(blueCount: 3, redCount: 2));

        //GameStatChangeInfo result = sut.Evaluate(skill, Team.Blue);

        //Assert.AreEqual(300, result.Blue.Att);
        //Assert.AreEqual(200, result.Red.Att);
    }
}
